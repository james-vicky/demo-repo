var name = `Vivekanand`; //you can use these also instead of double quotes, injecting of variables possible 
var fullName = `${name} Vernekar`;

console.log(fullName);
console.log(fullName.charAt(0)); //character at index
console.log(fullName.endsWith("?"));
console.log(fullName.includes("Vivekanand")); //does the string includes the specified character
console.log(fullName.toUpperCase());
